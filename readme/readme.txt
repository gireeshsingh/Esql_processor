Rules:
1.	Operator are {+,-,/,|,&}
2.	Follow the given input format
3. 	Output will be shown on console and also saved in queryOutput folder with the same name as input file
4.	Enter all predicates inside parenthesis Example: (a < b), (c = a), ((1_sum_quant>(2*2_sum_quant))|(1_avg_quant>3_avg_quant))
5.	Enter "all" instead of "*"
6.  First run the RunQPCreator which will create the QueryProcessor, InputRow and OutputRow		
7.  Now run the RunQueryProcessor which will run the user query